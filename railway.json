{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh /usr/local/bin/entrypoint.sh",
    "preDeployCommand": "php artisan migrate --force && php artisan db:seed --class=RoleAndPermissionSeeder --force",
    "healthcheckPath": null
  }
}